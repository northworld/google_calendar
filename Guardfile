# guard 'minitest' do
#   # with Minitest::Unit
#   watch(%r|^test/(.*)\/?test_(.*)\.rb|)
#   watch(%r|^lib/(.*)([^/]+)\.rb|)  { 'test' }
#   watch(%r|^test/mocks(.*)([^/]+)\.json|)  { 'test' }
#   watch(%r|^test/helper\.rb|)      { 'test' }
# end
